install.packages(c(
  "tidyverse",
  "readxl",
  "janitor",
  "scales"
))

# Bonus detentions lesson only:
# install.packages("nanoparquet")

cat("All packages installed. You're ready to go.\n")
cat("Open ddp-workshop.Rproj in RStudio before running any lesson files.\n")
