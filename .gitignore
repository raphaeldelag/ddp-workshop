# R
.Rhistory
.RData
.Rproj.user/
*.Rcheck/

# Raw FOIA data — too large to commit, download separately
data/raw/*.xlsx
data/raw/*.csv
data/raw/*.parquet

# Processed/derived data (e.g. DDP detention parquet) — also too large to commit
data/processed/*.parquet
data/processed/*.csv

# Quarto render output
_site/
_book/
*.html
*_files/

# Instructor-only — not for public repo
reference/instructor_code_guide.md

# OS
.DS_Store
Thumbs.db

/.quarto/
**/*.quarto_ipynb
